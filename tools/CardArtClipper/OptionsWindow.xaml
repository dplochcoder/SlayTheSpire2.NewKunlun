<Window
  x:Class="CardArtClipper.OptionsWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Options"
  Width="360"
  Height="210"
  ResizeMode="NoResize"
  WindowStartupLocation="CenterOwner"
>
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="110" />
    </Grid.ColumnDefinitions>
    <TextBlock Text="Application font size" VerticalAlignment="Center" />
    <TextBox x:Name="FontSizeBox" Grid.Column="1" Margin="8,3" />
    <TextBlock Grid.Row="1" Text="Gallery thumbnail size" VerticalAlignment="Center" />
    <TextBox x:Name="ThumbnailSizeBox" Grid.Row="1" Grid.Column="1" Margin="8,3" />
    <TextBlock
      Grid.Row="2"
      Grid.ColumnSpan="2"
      Margin="0,10"
      Foreground="#777"
      Text="Thumbnail size is measured in pixels. Changes apply immediately after saving."
      TextWrapping="Wrap"
    />
    <StackPanel
      Grid.Row="3"
      Grid.ColumnSpan="2"
      Orientation="Horizontal"
      HorizontalAlignment="Right"
    >
      <Button Content="Cancel" Width="80" Margin="4" IsCancel="True" />
      <Button Content="Save" Width="80" Margin="4" IsDefault="True" Click="Save_OnClick" />
    </StackPanel>
  </Grid>
</Window>
